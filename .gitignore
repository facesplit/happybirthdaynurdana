node_modules
dist
.DS_Store
*.log
test-results
playwright-report
tests/screenshots

# Исходные (приватные) фото/материалы — в публичный репозиторий НЕ кладём.
# На сайте используются уже обработанные версии в public/photos.
assets/

# Спрайты Stardew и музыка ВКЛЮЧЕНЫ в деплой, чтобы сайт работал как задумано
# (личный, некоммерческий подарок). Если нужна «чистая» публичная версия —
# раскомментируй строки ниже, и сайт откатится на оригинальный арт + генеративную музыку.
# public/sprites/*
# !public/sprites/.gitkeep
# public/audio/*.mp3
